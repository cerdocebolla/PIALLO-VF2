local.function = findableWiFi

if else 
then askForChild.findableWiFi.WiFi = activateMode (findableWiFiMode)

                if else 
                then askForParent.publicWiFi = findableWiFi|

            afterActivate = true 
            else (afterActivate.false) = false 
        then runError and 

        print ("An error has occured. Please try again.") = alwaysSetTrue.false 

        if else then (alwaysSetTrue.false) = false

        or (alwaysSetFalse) = false 

        else.true = tryActivate (alwaysSetTrue) = true 

        if goUp (insideBracket(end)) = true then 

    print ("Connected to the local WiFi successfully.")

    else endInstant.end
end)