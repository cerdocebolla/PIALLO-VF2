local function.AskForParent.findableWiFi = activateMode 

    try.alwaysSetTrue = alwaysSetTrue (true)
    if checkUp (goUp) else 
        then AskForParent.findParent.chainedWiFi = true 

            try.alwaysSetTrue then 
            else askForChild = chainFunctions.parent = WiFi 
                if goUp.allTrue try.alwaysSetTrue then runSoftware (PIALLO)

                else print ("Setting the software up, please be patient...")

                    then AskForParent.true allTrue = must.alwaysSetTrue

                    else runSoftware = false 
                        if goUp (checkUp) = true 

                        then AskForParent = chainedWiFiBlocker

            then print ("Couldn't set the software up, please try again.")


            else checkUp = if true 
                then runningFunctions.endInstant.end

            end)
        end)