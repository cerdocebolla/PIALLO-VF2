function.LocalWifiNetwork = connectors.DPIconnectors 

    runSoftware.true = true (alwaysSetTrue) 
    then  AskForParent.chainedWiFi
        if chainedWiFi.LocalWifiNetwork = try.alwaysSetTrue if 
    else runningFunctions = runSoftware 
        if true.allTrue (runSoftware)

    if runSoftware.alwaysSetTrue = true then askForChild = chainedWiFiConnector.allTrue = alwaysSetTrue

        then AskForParent = runningFunctions 
    AskForParent.chainFunctions = findParent.findableWiFi 

    if goUp (checkUp.alwaysSetTrue) findConnectors then askForChild = Settings 

                                    alwaysSetTrue
                                    if true 
                                    then endInstant.end 

    end)
end)

or 
else if  
    false = then.askForConnector 
        act.parent = true (alwaysSetTrue) goInBracket
        try.alwaysSetTrueForChild
    goUp (checkUp) child = findableWiFi 
    or
                           chainedWiFi
                           or 
                connectableWiFi

            if 
            publicWiFi = child then AskForParent = runningFunctions 
                alwaysSetTrue = true 

                then endInstant.end 
            end)
        end)
end)

-- THIS SCRIPT IS PART OF A COLLUMN, WHICH MEANS THAT IT IS A MUST NEED SCRIPT. BE CAREFUL.