if local.WiFiNotFound.true 
   try.alwaysSetTrue if not 

   then askForParent.findableWiFi = true 
   then acticateMode.findableWiFiMode = alwaysSetTrue
if not 

then else print ("False movement detected. Please try again.")

        if acticateMode.falseMovementBlocker 
        then askFor.falseMovementBlocker = blocker.true 

        try.alwaysSetTrue then askForChild.localFunction.client

    runningByTheClient = blocker.WiFiBlocker.false try.alwaysSetFalse = true 
    else then askForChild.localWifi 

    if not (true(orElse)) or askForParent.acticateMode = alwaysSetFalse 

    then print ("False movement detected, shutting the software down...")

         if not 
         else then askForChild.chainedWiFi.networkClient = WiFiUser.falseMovementBlocker = WiFiBlocker try.alwaysSetTrue

         if goUp.insideBracket = false 
    then print ("False movement has been removed, enjoy using PIALLO.")

    if falseMovementBlocker.true then try.alwaysSetTrue 

                                                    then.findParent.publicWiFi 
                                                    or
                                                    then.findParent.chainedWiFi = checkLineUp 
                                                         then endInstant.end
                                    end)
                end)
end)